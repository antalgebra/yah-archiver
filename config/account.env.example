# Copy this file to /etc/yah-arch/accounts/<account>.env on the server.
# Keep the real file out of Git and readable only by root and yaharch.
YAHOO_USERNAME=your-address@yahoo.com
YAHOO_APP_PASSWORD=replace-with-a-yahoo-app-password
POLL_SECONDS=20
RETRY_SECONDS=30
